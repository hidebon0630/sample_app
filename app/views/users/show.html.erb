<div class="md-page">
  <ul class="post-list jscroll">
  <%= @user.name %>
  <% if @user.posts.any? %>
    <h3>posts (<%= @posts.count %>)</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
    <%= paginate @posts %>
  <% end %>
  </ul>
</div>
