<% flash.each do |msg_type, msg| %>
  <div class="alert alert-<%= bootstrap_alert(msg_type) %>" role="alert" id="alert">
    <a href="#" class="close" data-dismiss="alert">×</a>
    <%= msg %>
  </div>
<% end %>
