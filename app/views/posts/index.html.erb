<% provide(:title, '全ての投稿') %>
<div class="md-page">
  <ul class="post-list jscroll">
    <% @posts.each do |post| %>
      <% if post.image? %>
        <li><%= link_to image_tag(post.image.url), post %></li>
      <% end %>
    <% end %>
    <%= paginate @posts %>
  </ul>
</div>
