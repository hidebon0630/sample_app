<% provide(:title, 'いいねランキング') %>
<ul class="post-list jscroll">
  <% @posts.each_with_index do |post, index| %>
    <%= render "ranking", post: post, index: index %>
  <% end %>
</ul>
<%= render "post_btn" %>
