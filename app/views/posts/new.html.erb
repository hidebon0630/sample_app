<div class="new_post_page border border-primary">
<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="form-group">
    <%= f.label :title, class:"bmd-label-floating" %>
    <%= f.text_field :title, class: 'form-control', required: true %>
    <span class="bmd-help">30文字以内で入力してください</span>
  </div>
  <div id="options">
    <%= f.fields_for :options do |option| %>
      <%= render 'option_fields', f: option %>
    <% end %>
    <div id="links">
      <%= link_to_add_association '質問を追加', f, :options %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :tag_list, class:"bmd-label-floating" %>
    <%= f.text_field :tag_list, value: @post.tag_list.join(','), data: {role: "tagsinput"} %>
  </div>
  <div class="form-group">
    <%= f.label :image, class:"bmd-label-floating" %>
    <label class="form-control">
    <% if @post.image.present? %>
      <%= image_tag @post.image_url, id: :img_prev, :size => '400x400', class: "form-control" %>
    <% else %>
      <%= image_tag "noimage.png", id: :img_prev, :size => '400x400', class: "form-control"  %>
    <% end %>
    <br>
      <span class="btn btn-raised btn-secondary btn-block">画像ファイルを追加する</span>
      <%= f.file_field :image, accept: 'image/jpeg,image/gif,image/png', id: :post_img, class: "d-none" %>
    </label>
  </div>
  <div class="form-group">
    <%= f.label :status, class:"bmd-label-floating" %>
    <%= f.select :status, Post.statuses.keys.map {|k| [I18n.t("enums.post.status.#{k}"), k]}, {}, class: 'form-control' %>
  </div>
  <%= f.submit "投稿", class: "btn btn-primary btn-raised btn-block" %>
<% end %>
</div>

<script type="text/javascript">
  $('#post_image').bind('change', function() {
    var size_in_megabytes = this.files[0].size/1024/1024;
    if (size_in_megabytes > 5) {
      alert('Maximum file size is 5MB. Please choose a smaller file.');
    }
  });
</script>

<script type="text/javascript">
  $(function() {
    function readURL(input) {
        if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
    $('#img_prev').attr('src', e.target.result);
        }
        reader.readAsDataURL(input.files[0]);
        }
    }
    $("#post_img").change(function(){
        readURL(this);
    });
  });
</script>
